# Packages

Shared code should live here when it becomes useful across clients or services.

Current reserved subfolders:
- `shared/`
- `swift/`

Current shared assets:
- `shared/contracts/` for machine-readable cross-runtime sync payload schemas
