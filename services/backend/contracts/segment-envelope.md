# Segment Envelope Contract Reference

The backend should treat `Docs/DevCoordination/sync-contract.md` as the source of truth for the first sync slice.

This local file exists only as a backend-side reminder:

- first mandatory sync unit: `SegmentEnvelope`
- review should initially be folded into segment state
- collections/exports are later extensions unless explicitly promoted
